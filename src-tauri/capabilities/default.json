{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window: events + path. Custom app commands are allowed by default.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
